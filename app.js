const shoppingCartBtn = document.getElementById("shopping-cart-button");

shoppingCartBtn.addEventListener("click", () => {
  const quantity = document.getElementById("shopping-cart-quantity");
  let qnt = parseInt(shoppingCartQnt.innerText) || 0;
});
